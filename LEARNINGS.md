# SGIS 학습 로그 (LEARNINGS)

> **AI 진화 노트.** SGIS를 쓰며 겪은 **실패·성공·함정**을 누적한다.
> 이 파일은 SGIS 스킬의 "오답노트"다. 매 작업 전에 읽고, 새로 배운 것을 매 작업 후에 적어 스스로 진화한다.

## 사용 규칙 (AI 필독)

1. **조회 전 READ**: SGIS 작업을 시작하면 이 파일을 먼저 읽는다. 특히 `## 엔드포인트별 함정`과 `## 오류코드 대응`을 확인해 같은 실수를 반복하지 않는다.
2. **작업 후 APPEND**: 주목할 만한 **실패**(예상 못한 오류·빈 결과)나 **성공**(처음 통한 쿼리·해결한 함정)을 겪으면 아래 해당 섹션에 **한 줄 이상** 추가한다.
3. **형식 유지**: 각 항목은 `날짜 | 엔드포인트/명령 | 무슨 일 | 왜 | 다음엔 이렇게` 5요소를 가능한 한 채운다. "왜"가 가장 중요하다.
4. **중복 금지·승격**: 이미 있는 항목이면 새로 쓰지 말고 기존 항목을 보강한다. 같은 함정이 3회 이상 반복되면 `## 일반 규칙`으로 승격한다.
5. **검증된 것만 성공으로**: 실제 실행해 데이터가 온전히 나온 쿼리만 `## 성공 쿼리 패턴`에 올린다(추측 금지).

---

## 일반 규칙 (반복 검증된 핵심)

- **`accessTimeout`은 밀리초 단위 Unix epoch 절대시각** — 문서에는 "1970-01-01 0시부터 현재까지의 초"라고 되어 있으나 실측 결과 13자리 ms epoch. 예: `"1782022524954"` = 밀리초. `time.UnixMilli()` 또는 `/1000` 후 `time.Unix()` 사용. "초"로 취급하면 파싱 버그 발생.
- **기본 좌표계는 UTM-K (EPSG:5179)** — 통계 stage 좌표, boundary geojson 좌표, geocode/rgeocode 기본형이 모두 UTM-K(미터 단위, x≈95만/y≈195만 범위). WGS84(경위도)가 필요하면 `geocodewgs84`/`rgeocodewgs84` 전용 엔드포인트 사용. 일반 boundary .geojson은 WGS84 미제공.
- **accessToken은 쿼리파라미터로 전달** — 헤더가 아닌 `?accessToken=<TOKEN>` 쿼리스트링. SGIS 모든 데이터 API 공통.
- **토큰 유효기간 ≈ 4시간** — 실측: `accessTimeout - now ≈ 14,400,000 ms`. 적응형 재발급 임계: `max(60s, 0.1 * 14400s) = max(60s, 1440s) = 24분` 전 자동 갱신.
- **adm_cd 자릿수**: 미지정=전국, 2자리=시도, 5자리=시군구, 8자리=읍면동. 자릿수가 틀리면 조용히 빈 결과 또는 전국 데이터 반환.
- **year 범위는 엔드포인트마다 다름** — 인구/주택 2015~2024, 사업체 2000~2024, 농림어가 2000/2005/2010/2015/2020(5년 주기). 범위 밖 연도 요청 시 빈 결과.

---

## 엔드포인트별 함정

| 날짜 | 엔드포인트/명령 | 함정 | 왜 | 다음엔 |
|------|----------------|------|-----|--------|
| 2026-06-21 | `auth/authentication.json` | `accessTimeout` 필드를 `int64`로 파싱하면 `"cannot unmarshal string into Go struct field"` 오류 | 응답이 `"1782022524954"` 형태의 **JSON 문자열**로 옴 (숫자 아님) | `string`으로 수신 후 `strconv.ParseInt` 변환. auth.go의 struct 태그 주의 |
| 2026-06-21 | `addr/stage.json` | 미지정(`cd` 없음) 시 시도 17개 반환. `cd=11` 시 서울 구 25개 반환 | 단계별 조회 구조 — cd 자릿수에 따라 반환 단계가 달라짐 | 시도=cd 없음, 시군구=2자리, 읍면동=5자리 |
| 2026-06-21 | `boundary/hadmarea.geojson` | Leaflet 지도에서 경계가 엉뚱한 위치(보통 아프리카 해상)에 표시됨 | 좌표가 UTM-K(x≈953932, y≈1952053) — Leaflet은 WGS84(lon, lat) 기대 | `transcoord` API로 EPSG:5179→4326 재투영 후 사용하거나, 지도 라이브러리에 proj4 추가 |

---

## 성공 쿼리 패턴 (실측 검증됨)

| 날짜 | 명령 | 비고 |
|------|------|------|
| 2026-06-21 | `sgis code stage` | 시도 17개 목록 정상 반환. cd 미지정 = 시도 레벨 |
| 2026-06-21 | `sgis code stage --cd 11` | 서울 구 25개 반환 |
| 2026-06-21 | `sgis data population --adm-cd 11 --year 2020` | 서울 2020 인구: `tot_ppltn=9586195`, `tot_family=3982290`, `avg_age=42.4` |
| 2026-06-21 | curl `auth/authentication.json?consumer_key=...&consumer_secret=...` | 토큰 발급 성공. 응답: `{result:{accessToken:"...", accessTimeout:"1782022524954"}, errCd:0}` |

---

## 오류코드 대응

| errCd | 의미 | 즉효 대응 |
|-------|------|-----------|
| `0` | 성공 | — |
| `-401` | 인증 실패 (consumer_key 오류 등) | consumerKey/consumerSecret 재확인. `sgis config get`으로 설정값 점검 |
| 비-0 일반 | 요청 거부 | `errMsg` 내용 확인 후 파라미터 수정 |
| HTTP 429 | 호출 빈도 초과 | 자동 지수 백오프(1·2·4s, 3회). 잦으면 요청 간격 늘리기 |
| 네트워크 오류 | 서버 연결 실패 | 재시도 가능. `ErrNetwork` 분류 — credential 오류와 다름 |

---

## 성능 팁 (실측)

- 토큰 캐시(`~/.sgis/token.json`)로 매 명령마다 재발급 없음. 4시간 유효.
- boundary geojson은 대용량일 수 있음 — `-o` 파일 출력 권장 (stdout 파이프 시 메모리 주의).
- `low_search=0`이 기본인 엔드포인트에서 하위 단위 보려면 `--low-search 1` 명시.

---

<!-- 새 학습은 위 해당 섹션에 추가하세요. 형식: 날짜 | 엔드포인트/명령 | 무슨 일 | 왜 | 다음엔 이렇게 -->
